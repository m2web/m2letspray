//This was needed using the loginButtons helper.
//Since we are not using that now, but using a custom login with Facebook's api, it is not needed.
/*
Accounts.ui.config({
	passwordSignupFields: 'USERNAME_ONLY'
});
*/